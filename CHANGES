в 3.4 версии по сравнению с тем что было для виндов раньше

1) добавился примитивный рисовальщик карт. (_маппер номер_зоны.) Хорош для изучения зон на локале :P рисует во 2 окно 
	чтобы потестить можно попробовать _маппер 499 - это нк.

2) добавился пустой пока пкмодуль. доделаю мб когда нить

3) добавились автопечать (_печатать) + автоогляд (_оглядывание)

4) поправил автопарир и прочее, вроде не глючит

5) добавил нормальное автовставание (чтото глючит но что никак не пойму)

6) добавил нормальный групрекол, клонами или еще там чем. добавление в список вместе с автореском. кнопка C-[ (спасибо f@n у)

7) убрал то что раньше называлось битьвсех, теперь есть не 6 атак а 9 (кнопочек). 

8) теперь кнопочка атаки + контрол есть применение этой атаки по всем целям (замена тому что раньше называлось битьвсех)
	теперь кнопочка атаки + альт есть применение этой атаки по всем целям в обратном направлении (с конца)

9) добавил две кнопки для (Танк Вверх|Вниз). вешаецца на контрол + (Танк Север|Юг) 

10) добавил команду _шаблон_управляющего. По умолчанию (ТАНК ->). То есть то что в конфе.

11) добавил режимы лута 
	(лут 1) лут из трупов (если моб рассыпался в прах будет взять все)
	(лут 2) то что было раньше (лут трупов в инв)
	(лут 3) лут чармисами трупов (чернокам). задержка 1 секунда

11.5) и еще для черноков написал команду 'фс' - трупы жрать

12) добавил автопрописывание !фликов. одевать их за вас никто не будет.

13) вроде бы пофиксил режим подкрадывания. +команда (поя) его отключает.

14) добавил кнопку k7 (идти туда куда ушел танк) + автоматически смотрит за танком (спасибо f@n у)

15) на k5 теперь оглядывание. (то f@n : печать с C-k5 убрал пришлось убрать. там фичи с огл... повесь на какуюнить атаку и пусть k9 например)

16) добавил кривые подсветки на взятие трупов и хождение персонажей (спасибо f@n у)

17) пофиксил подсчет икспы. (все равно чтото глючит никак не пойму что)

18) добавил запрет вхождения в дт. (не помню но вроде в виндовс версии не было этого еще)

19) добавил нормальное переключение в башмод/дуалмод/двур. Все прописываецца автоматом по экипировке
	примечание - в сумку ничего не кладет

20) добавил нормальное применение посоха. теперь работает в режимах пунктом выше а не только в дуале. (по умолчанию посох - посох)
	примечание - в сумку ничего не кладет

21) добавил проверку на наличие рекола после рекола. Отсутствие рекола теперь сложно не заметить. 

22) пофиксил команду смены контейнера (наконецто)

23) поменял команды с пищей. теперь там без изысков и все надо печатать полностью.

24) добавил окошко для теллов. кнопка ins переключает туды сюды.

25) добавил кучку спидволков. (в том числе спидволки от базара дсс)

26) изменил дизайн подсветок

27) алиасы оружия теперь прописываюцца по базе. глюкам нет.

28) пофиксил непонятные баги при коннекте. вроде бы.

29) поменял названия файлов логов + убрал timestamp в логах.

30) пофиксил все известные баги с цифрами (полет 0) + добавил в группу еще 1 колонку с ними.

31) пофиксил волхвовский конфиг. (опять же спасибо f@n у за кусок кода с командой '_комплект')

32) убрал сообщение 'Забито'. неактуально

33) слегка изменился файл aliases.rc, 
	добавил порт для команды подключения к локалхосту (теперь называецца (подключение к localhost)) 
	добавил (порт на localhost)
    если просто скопировать старый то команда 'лб' перестанет работать

34) добавил запрет (мягкий) на постановку пенты менее чем за 15 секунд до тика (заколебало ставить пенты 2 раза)
        в связи с этим в конфигах к профам изменился алиас на пенту с 'переход' на '_переход'. 'переход' теперь алиас конфига

35) пофиксил все баги которые знал. добавил новых.
